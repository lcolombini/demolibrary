{
    "scripts": {
        "test": "echo \"Error: no test specified\" && exit 1",
        "dev": "nodemon api/server.ts",
        "build": "rm -rf build && npx tsc",
        "run": "node build/api/server.js"
    },
    "devDependencies": {
        "typescript": "^5.1.6"
    },
    "dependencies": {
        "@types/express": "^4.17.17",
        "@types/morgan": "^1.9.4",
        "dotenv": "^16.3.1",
        "express": "^4.18.2",
        "morgan": "^1.10.0"
    }
}
