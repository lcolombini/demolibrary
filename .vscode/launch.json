{
    "version": "0.2.0",
    "configurations": [
        {
            "type": "node-terminal",
            "request": "launch",
            "name": "Launch via NPM",
            "cwd": "${workspaceFolder}",
            "command": "npm run dev"
            
        }
    ]
}